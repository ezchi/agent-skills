# Regression Test Best Practices

- treat each test as independent
- avoid global state
- produce textual PASS/FAIL token
- archive waveforms only on failure
- include random seeds in logs
- allow parallel execution
